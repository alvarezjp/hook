// @ts-check 

/**
 * nombre de usuario
 * @type {string}
 */

let userName = "humberto1"
let algo = 123;

/**
 * @type {Array<number|string|boolean>}
 */

const arreglo = [1,2,3,5,"algo",true]

/**
 * @typedef {object} User
 * @property {string} name
 * @property {number} age
 * @property {boolean} algo
 */

/**
 * @type {User}
 */
const user2 = {
    name:"22",
    age:22,
    algo:false
} 